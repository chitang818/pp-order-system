# Puppeteer配置 - 将Chromium下载到项目本地
# 这样复制项目到其他电脑时，不需要重新下载Chromium
# 
# 注意：PUPPETEER_CACHE_DIR 和 PUPPETEER_SKIP_DOWNLOAD 已改为环境变量配置
# 这些配置现在在启动脚本中通过环境变量设置，不再在 .npmrc 中配置
# 
# Chromium下载镜像（可选，加速国内下载）
# PUPPETEER_DOWNLOAD_HOST=https://registry.npmmirror.com/-/binary

